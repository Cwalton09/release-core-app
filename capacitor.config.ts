import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.chelseawalton.releasecore',
  appName: 'Release Core',
  webDir: 'out',
};

export default config;